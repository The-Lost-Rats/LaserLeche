using UnityEngine;

public class Constants
{
    public const int SCREEN_SIZE = 64;
    public const int PIXELS_PER_UNIT = 4;
    public const int SCREEN_BOUNDS = (SCREEN_SIZE / PIXELS_PER_UNIT) / 2;

    public const float PARALLAX_DISTANCE_SCALE_FACTOR = 1.0f;
}
